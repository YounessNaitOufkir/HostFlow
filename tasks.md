# Cursor Implementation Plan: HostFlow Refinements

**Role:** Expert Software Architect
**Context:** I am building HostFlow, a bilingual work management application designed for teams that run repetitive processes. The app features strict granular access controls where company work and private personal work coexist securely.
**Current Stack:** Next.js 16 (App Router), React 19, TypeScript, Supabase (Postgres, RLS), TanStack Query, Tailwind CSS, Vercel.
**Feature to build:** Six critical infrastructure and UX improvements: Audit Trail, Trash Purge Job, Workspace Automations, Multi-Tab Real-Time Sync, Strict Assignee Privacy Filtering, and External Email Invites.

---

## 1. Feature Overview & Definition of Done

**Goal:** Implement six missing architectural features to finalize the platform's access model, operational hygiene, and real-time user experience.

**Definition of Done:**
- **External Email Invites:** Users (both staff and external) can invite new collaborators to a workspace by typing their email address. If the user doesn't exist, they receive a signup link that auto-grants access upon account creation.
- **Strict Assignee Privacy Filtering:** In private workspaces, the assignee dropdown is strictly locked down to show only the current user and existing assignees, preventing the app's directory from leaking into personal workspaces.
- **Audit Trail:** Item creation/deletion, status, assignee, description, due date, and priority changes are logged to a new Postgres table. An "Activity Feed" tab exists on the Board view, accessible only to admins, strictly respecting privacy (admins cannot see logs of private workspaces).
- **Trash Purge Job:** Soft-deleted items older than 30 days are permanently purged via an automated cron job, and their associated Supabase Storage file attachments are deleted securely.
- **Per-Workspace Automations:** The Workspace Settings modal contains an "Automations" tab allowing users to configure automation rules scoped by `workspace_id`.
- **Multi-Tab Sync:** Modifying data in one browser tab instantly refreshes TanStack Query data in all other open tabs on the same origin via `BroadcastChannel`.

---

## 2. Architecture & Data Model

- **Audit Logs (`audit_logs` table):** 
  - Columns: `id`, `board_id`, `user_id`, `action_type`, `old_value`, `new_value`, `created_at`.
  - **RLS Policy:** Must duplicate the strict `can_access_workspace_as()` logic. Admins get access, but *only* if the workspace is non-private or they are explicitly invited.
- **Trash Purge Strategy:** A Next.js API route (`/api/cron/purge-trash`) triggered by Vercel cron. It must query expired items, delete their files via `@supabase/supabase-js` storage API, and then delete the Postgres rows.
- **Automations Data:** Reuse the existing `automations` table logic, but adapt the UI to set `workspace_id` instead of `board_id`.
- **Real-Time Sync:** Leverage the existing `notifyTabSync()` helper in `useRealtimeSync.ts` to dispatch events across the `BroadcastChannel`.

---

## 3. File Touchpoints

The following literal file paths will need to be created or modified:

- `supabase/migrations/[timestamp]_create_audit_log.sql` (New)
- `components/views/ActivityLog.tsx` (New)
- `components/ItemPanel.tsx`
- `app/api/cron/purge-trash/route.ts` (New)
- `vercel.json`
- `components/WorkspaceDialog.tsx`
- `components/WorkspaceAutomations.tsx` (New)
- `hooks/store/useItemMutations.ts`
- `hooks/store/useBoardMutations.ts`
- `hooks/store/useColumnMutations.ts`
- `hooks/store/useGroupMutations.ts`
- `hooks/store/useWorkspaceMutations.ts`

---

## 4. Implementation Steps (The Checklist)

- [x] **Step 1: Database Migration for Audit Logs**
  - **Files:** `supabase/migrations/20260908000000_create_audit_log.sql`, `scripts/rls-audit.mjs`
  - **Logic:** Created the `audit_logs` table (distinct from the existing `activity_logs`, which is a board-member-visible feed, not an admin oversight trail — see the migration's header comment). Items store fields in `column_values` jsonb keyed by dynamic per-board column ids rather than fixed columns, so the trigger resolves "status/assignee/priority/due-date/description changed" by joining `boards.columns` for each changed key's `type`, mirroring the existing type-or-title pattern in `evaluateTimeAutomations`. RLS reuses the canonical `is_global_admin()` + `can_access_board()` predicates rather than restating the privacy rule (a second copy is what caused prior drift in this codebase). No INSERT/UPDATE/DELETE policy exists for `authenticated` — rows are written only by two `SECURITY DEFINER` triggers, and direct RPC access to those trigger functions is revoked.
  - **Verification:** Extended `scripts/rls-audit.mjs` with 6 new checks (admin sees audit rows on a reachable board incl. a `status_changed` row proving the jsonb-diff logic; admin sees none on a private board they don't belong to; a non-admin staff member sees none at all even with full board access; external sees none; nobody can INSERT a row directly). `npm run test:rls` → 32/32 passed, no regressions.

- [x] **Step 2: Activity Feed UI**
  - **Files:** `components/views/ActivityLog.tsx` (new), `components/layout/BoardHeader.tsx`, `app/page.tsx`, `hooks/store/types.ts`, `hooks/queries/queryKeys.ts`, `types/index.ts`, `lib/i18n/en.ts`, `lib/i18n/fr.ts`
  - **Logic:** Built as a board-level tab (per the Definition of Done), not embedded in `ItemPanel.tsx` — that file already has unrelated, unfinished, dead scaffolding for the *other* `activity_logs` table which this intentionally does not touch. The tab itself is gated `isAdmin` in `BoardHeader` (hidden entirely for non-admins, not just access-denied), and `ActivityLog.tsx` self-gates too via `useAuth()` as defense in depth, following the pattern in `TrashView.tsx`. Fetches `audit_logs` with an embedded `items(name)` join for item names, resolves actor names via the existing `useProfilesQuery()` (`user_directory`, already the safe/shared surface for this), and renders a human sentence per action type (bilingual, en/fr) with an icon, e.g. "Amine changed Status on 'Kickoff' from Working on it to Done."
  - **Verification:** `npm run build`-equivalent typecheck + lint clean on all changed files. Verified live in a real browser (Playwright) against the disposable `ZZ RLS` fixtures from Step 1: signed in as `rls-admin` — the Activity tab appears, opens, and correctly renders both seeded `status_changed` rows with resolved item name and old→new values, no console errors. Signed in as `rls-member` (non-admin, full board access) — confirmed the word "Activity" appears nowhere on the page; the tab is fully hidden, not just empty.

- [x] **Step 3: API Route for Trash Purge Job**
  - **Files:** `app/api/cron/purge-trash/route.ts` (new), `vercel.json`, `lib/cronHeartbeat.ts`
  - **Logic:** Follows the existing `automations`/`daily-digest` cron template exactly (same `Authorization: Bearer CRON_SECRET` fail-closed auth, same `createAdminClient()`, same `cron_runs` heartbeat via `startCronRun`/`finishCronRun`). Fetches items with `deleted_at` older than 30 days, resolves which of each item's board's columns are `files`-type, extracts the Storage object key back out of the public URLs `FilesCell.tsx` stores in `column_values` (there was no existing precedent for a `.storage.remove()` call anywhere in the codebase — this is the first one), deletes those objects, then deletes the item rows. Storage removal runs before the row delete deliberately: a purge that dies mid-run just leaves files for the next run to catch, rather than orphaning them with no row left to retry from. `lib/cronHeartbeat.ts`'s `CRON_MAX_SILENCE_HOURS` gets a `purge-trash` entry (26h, same as the other daily jobs — a weekly cadence would let the 30-day promise slip to 37) and `vercel.json` gets a matching `0 9 * * *` entry alongside `daily-digest`/`automations`.
  - **Verification:** Seeded a real soft-deleted item (31 days old) with a real uploaded file in a disposable scratch workspace, confirmed `401` for missing/wrong `CRON_SECRET`, then hit the endpoint locally via curl with the real secret → `{"success":true,"purged":1,"filesDeleted":1}`. Confirmed directly against the DB: the item row is gone, the Storage object is gone, and — proving Step 1 and Step 3 compose correctly — the item's `audit_logs` row **survived** with `item_id` set to `NULL` rather than being cascaded away, exactly as designed so the audit trail outlives the data it describes. `cron_runs` recorded `ok: true` with the matching detail. Scratch fixtures cleaned up afterward.

- [x] **Step 4: Workspace Automations UI**
  - **Files:** `components/WorkspaceAutomations.tsx` (new), `components/WorkspaceMembersModal.tsx`, `components/layout/Sidebar.tsx`, `hooks/queries/queryKeys.ts`, `lib/i18n/en.ts`, `lib/i18n/fr.ts`
  - **Logic:** `components/WorkspaceDialog.tsx` turned out to be the *new-workspace creation* prompt, not a settings dialog — there was no tabbed "Workspace Settings" surface to add to. `WorkspaceMembersModal.tsx` (opened via the sidebar's "Manage access" icon) is the actual per-workspace management dialog, so it gained Members/Automations tabs there instead, gated on a client-side mirror of `can_manage_workspace()` (creator, or a `workspace_members` row with role admin/manager, or a non-private-workspace admin — not a bare `role === 'admin'` check, which would have wrongly hidden the tab from a workspace manager the DB would actually allow in). The underlying capability already existed: `AutomationsModal.tsx` (the board-level "Automate" button) already writes `sla_alert`/`overdue_tagging` rules with `workspace_id` set and `board_id` null when a board belongs to a workspace, and `automations_for_board()` plus both cron/on-demand run routes already union board- and workspace-scoped rules — so this step only needed a board-independent entry point and list view, not new engine plumbing. `WorkspaceAutomations.tsx` deliberately offers only those same two recipes, never `move_group` (its target is a single board's group id, which a workspace has no equivalent of), with the same one-rule-per-action-type duplicate guard as the board modal (the engine takes the first matching rule; a second is silently shadowed).
  - **Verification:** Playwright, signed in as `rls-admin`: opened the `ZZ RLS Company` workspace's Manage Access dialog, switched to the new Automations tab, created a "due date alert" rule — toast confirmed, rule appeared with a "Daily 09:00" badge. Confirmed directly in the DB: `board_id: null`, `workspace_id` set to the real workspace id, exactly as required. Also verified the duplicate-guard (recreating the same recipe is blocked with a warning), toggle-off, and delete all work. `npm run test:rls` → still 32/32 after (a test-script mistake briefly flipped the shared fixture workspace's own Private/Shared toggle — same `role="switch"` selector, wrong element — caught and reverted immediately; not a defect in the shipped code).

- [x] **Step 5: Multi-Tab Real-Time Sync via Hooks**
  - **Files:** `hooks/useRealtimeSync.ts`, `hooks/store/useItemMutations.ts`, `hooks/store/useBoardMutations.ts`, `hooks/store/useColumnMutations.ts`, `hooks/store/useGroupMutations.ts`, `hooks/store/useWorkspaceMutations.ts`, `app/page.tsx`, `__tests__/lib/cronHeartbeat.test.ts`
  - **Logic:** A single `notifyTabSync(boardId)` call wasn't actually sufficient for three of the five files: `useItemMutations.ts` already called it for most (not all — `duplicateItem`/`restoreItem`/`permanentlyDeleteItem`/`addLink`/`updateLink`/`removeLink`/`handleDragEnd`'s item-move branch, and `renameItem`, were all missing it) and group data too, since `queryKeys.boardData(boardId)` covers items/groups/links/automations in one query — but board **columns** live directly on the `boards` row (`useColumnMutations.ts` always writes `boards.columns`), and workspace mutations have no `boardId` at all. Neither is covered by invalidating `boardData(boardId)`. So `hooks/useRealtimeSync.ts` gained two more broadcast types alongside the existing `SYNC_BOARD` — `SYNC_BOARDS` (`notifyTabSyncBoards()`, for board-list/column/item-name-column changes) and `SYNC_WORKSPACES` (`notifyTabSyncWorkspaces()`) — wired to the `onBoardsChanged`/new `onWorkspacesChanged` callbacks already passed from `app/page.tsx`. Also moved the `BroadcastChannel` listener out of the board-gated `useEffect` into its own always-mounted effect: it previously returned before subscribing whenever `activeBoard` was null, so a tab sitting on My Work, Trash, or Workspace Overview never received *any* cross-tab update, board or workspace. `useGroupMutations.ts` writes go through the existing `SYNC_BOARD` channel (group data is inside `boardData`); `useColumnMutations.ts` and `useBoardMutations.ts` (create/rename/delete/item-name-column) use the new `SYNC_BOARDS`; `useWorkspaceMutations.ts` (create/rename/delete) uses the new `SYNC_WORKSPACES`.
  - **Verification:** `npx vitest run` → 720/720 (one pre-existing test updated for the new `purge-trash` cron job key, unrelated to this step but caught by the full run). Playwright, two tabs signed in as `rls-admin` on the same `ZZ RLS Open` board: renamed a group in Tab 1 → Tab 2's Main Table view showed the new title with no manual reload (screenshot confirmed). A third tab with no board open at all confirmed both new broadcast types reach it too: a raw `SYNC_WORKSPACES` message triggered a real `workspaces` refetch, and `SYNC_BOARDS` triggered a real `boards` refetch — proving the board-gated-listener fix. All test fixtures reverted; `npm run test:rls` → still 32/32 after.

- [x] **Step 6: Assignee Access Guard (UX/UI)**
  - **Phase 1: Design Proposal:** Published a design artifact with 3 mocked options (inline-in-dropdown / centered modal / anchored non-blocking card) before writing any code. User picked the anchored card, with a mockup CSS bug (`white-space: nowrap` leaking from a `.mock-cell` ancestor) called out and fixed before proceeding.
  - **Files:** `components/cells/PeopleCell.tsx`, `components/ui/AssigneeAccessPrompt.tsx` (new), `components/BoardAccessContext.tsx` (new), `lib/boardAccess.ts` (new), `hooks/queries/useBoardAccessQuery.ts` (new), `hooks/store/useBoardMutations.ts`, `hooks/useBoardStore.ts`, `app/page.tsx`, `types/index.ts`
  - **Logic:** The picker in `PeopleCell.tsx` already hides inaccessible people via `useAssignablePeople()` on shared workspaces — the real gap is private boards, where that context is `null` by design (a private board is how an external gets invited) and every profile becomes clickable. `lib/boardAccess.ts` mirrors `can_access_board_as()`/`can_manage_board()` client-side for an arbitrary candidate, not just the session user, using a new `board_members`/`workspace_members` query (`useBoardAccessQuery`, RLS already lets anyone who can open the board read both in full) fed through a new `BoardAccessContext` (same rationale as the existing `AssignablePeopleContext`: needed several layers below where the board/membership data lives). The guard fires only on adding a new assignee, never on removing one, and only when `hasBoardAccess` is false. "Grant Access" is gated on `canGrant` (mirroring `can_manage_board`) so it's never offered to someone whose insert would just no-op under `BoardMembers: Insert` RLS; the insert itself uses the same `.select("id")` read-back-verification pattern as every other membership write in this codebase.
  - **Verification:** Full `npx tsc --noEmit` and `npm run build` clean; `npx vitest run` → 720/720. Live Playwright against a disposable private workspace/board (owned by the `rls-member` fixture, so `canGrant` is true) run against a **separate production server on port 3011** — `next dev` refuses a second instance for the same project, and the user's own dev server on port 3000 was left untouched throughout. Assigning a non-member correctly opened the anchored card instead of assigning silently; "Grant & assign" produced a real `board_members` row (`role: member`) and added the id to the item's `column_values`; a second candidate via "Assign anyway" was added to `column_values` with **no** `board_members` row, confirmed both by screenshot and directly against the DB. `npm run test:rls` still 32/32 after; scratch fixtures deleted.

- [x] **Step 7: Enforce Strict Privacy Filtering in Assignee Dropdown (Replacing Step 6 UX)**
  - **Files:** `components/cells/PeopleCell.tsx`, `app/page.tsx`, `hooks/store/useBoardMutations.ts`, `hooks/useBoardStore.ts`, `hooks/queries/queryKeys.ts`, `types/index.ts`, `lib/i18n/en.ts`, `lib/i18n/fr.ts`, `__tests__/components/cells/PeopleCell.test.tsx` — plus **deleted** `components/ui/AssigneeAccessPrompt.tsx`, `components/BoardAccessContext.tsx`, `lib/boardAccess.ts`, `hooks/queries/useBoardAccessQuery.ts`
  - **Logic:** A full revert, not a narrower private-only patch: once nobody without access can ever appear in the picker, there is nothing left for Step 6's guard card to warn about, so it and every file built only to support it (the board-access predicate mirror, its query, its context, the `grantBoardAccess` mutation) came back out rather than sitting as dead code behind an unreachable branch. `PeopleCell.tsx`'s `pickable` now reads: when `assignable` is non-null (shared workspace), unchanged; when `null` (private), filter to `u.id === currentProfile.id || selectedIds.includes(u.id)` — `currentProfile` from `useAuth()`, called directly in the cell the same way `TrashView.tsx` does rather than threaded down as a prop. Confirmed via lint that the revert is byte-for-byte equivalent to the pre-Step-6 baseline (identical 40-problem count, same lines).
  - **Verification:** `npx tsc --noEmit` and `npm run build` clean. `npx vitest run` → 720/720, including 9/9 in `PeopleCell.test.tsx` after fixing a test that directly asserted the now-reverted behavior ("offers everyone when there is no restriction") and adding an `AuthProvider` mock plus an explicit `AssignablePeopleContext` value to a write-survival test that had been incidentally relying on the old unrestricted default. Live Playwright (production server on port 3011, dev server on port 3000 left untouched throughout) against a disposable private workspace owned by the `rls-admin` fixture: opening the Assignee picker showed **only** "ZZ RLS Admin" (self) — confirmed by screenshot and by asserting none of four other real directory names (`Amine ABOUTALIB`, `ZZ RLS Member`, `E2E Test User`, `Yasser`) appear anywhere on the page. `npm run test:rls` → still 32/32; scratch fixtures deleted.

- [x] **Step 8: Invite by Email (External Invites)**
  - **Files:** `supabase/migrations/20260909000000_pending_invitations.sql` (new), `supabase/migrations/20260909000100_pending_invitations_grants.sql` (new, see below), `app/api/workspaces/invite/route.ts` (new, not a Server Action — see below), `components/WorkspaceMembersModal.tsx`, `app/login/page.tsx`, `types/index.ts`, `lib/i18n/en.ts`, `lib/i18n/fr.ts`, `scripts/rls-audit.mjs`, `__tests__/components/WorkspaceMembersModal.test.tsx`.
  - **Logic:** An API route rather than `actions/inviteToWorkspace.ts`: every comparable "write + third-party side effect" flow in this codebase (digest email, Telegram notify, on-demand automations) is already an `app/api/**/route.ts` handler using the same auth-then-elevate pattern (`createClient()` to identify the caller and check `can_manage_workspace` via RPC as *them*, then `createAdminClient()` scoped to what that one check just authorised) — the `actions/` directory's non-Telegram members turned out to be unused scaffolding with a stale schema, not a real convention to follow. `redeem_pending_invitations` is a trigger on `public.profiles`, not `auth.users`: this codebase already has exactly one `auth.users` trigger (`handle_new_user`), and same-table multi-trigger firing order is a fragile thing to depend on, whereas `handle_new_user` inserts into `profiles` as a plain statement, so a `profiles` trigger fires deterministically nested inside it, not raced. `WorkspaceMembersModal.tsx` gates the directory list on the *inviter's* own staff status (`profile.is_staff !== false`) — an external managing their own private workspace sees only the email field, never the company directory, matching the same privacy principle Step 7 established for the assignee picker.
  - **A deployment gap surfaced applying this migration**: the new table came back "permission denied" (not an RLS-empty-result) after the first apply — the default-privilege grants every other table in this schema picks up automatically hadn't applied to this one. Fixed with a small follow-up migration granting `authenticated`/`service_role` explicitly.
  - **Verification:** `npx tsc --noEmit`, `eslint`, `npm run build` clean; `npx vitest run` → 721/721 (fixed one test's Supabase mock that didn't support the new query's `.order()` chain). Extended `scripts/rls-audit.mjs` with 5 new checks (a private workspace's own creator can invite into it; an admin cannot invite into someone *else's* private workspace; an external cannot invite into the company workspace; an external reads no pending invitation at all; revoke works) → 37/37. Direct trigger test (service-role, real `auth.users` accounts, bypassing the UI entirely): confirmed `workspace_members` granted, `is_staff` flipped only when `is_staff_invite` was true, the consumed invite row deleted, and `handle_new_user`'s own default-workspace creation for the new user unaffected. Live Playwright: staff inviter sees both the directory and the email field; inviting an existing account's email granted access immediately (screenshot shows "Has access"); inviting a new email produced a working Pending-list entry with Revoke; an external inviter's view showed *only* the email field, confirmed no staff name leaks; `/login?invite=...` correctly switched to signup and showed the banner. Scratch fixtures deleted throughout.

- [x] **Step 9: Restore Assignee Access Guard for Shared Workspaces**
  - **Files:** Rewrote `components/ui/AssigneeAccessPrompt.tsx`, `components/BoardAccessContext.tsx`, `lib/boardAccess.ts`, `hooks/queries/useBoardAccessQuery.ts` (these were created and deleted entirely within the same uncommitted working tree in Steps 6/7 — never actually committed, so there was no git history to restore from; rebuilt from the Step 6 content verbatim). Also `components/cells/PeopleCell.tsx`, `hooks/store/useBoardMutations.ts`, `hooks/useBoardStore.ts`, `hooks/queries/queryKeys.ts`, `types/index.ts`, `app/page.tsx`, `lib/i18n/en.ts`, `lib/i18n/fr.ts`, `__tests__/components/cells/PeopleCell.test.tsx`.
  - **Logic:** The merge point is `assignable === null` in `PeopleCell.tsx` — the existing `useAssignablePeople()` signal already distinguishes private (null) from shared (a `Set`) workspaces, so no new "is this workspace private" check was needed. Private stays exactly as Step 7 left it (`pickable` filtered to self + already-assigned, guard never consulted — nothing it offers can lack access, since seeing the picker at all already implies board access). Shared workspaces keep their existing directory-filtered `pickable` unchanged, but `toggleUser` now checks `boardAccess.hasAccess(userId)` for a newly-added (not already-selected) pick and shows the guard instead of assigning directly when it's false — restoring Step 6's card exactly, gated to only the branch where it can actually fire.
  - **Verification:** `npx tsc --noEmit`, `eslint`, and `npm run build` clean (confirmed the restored files reproduce the exact same lint-baseline shape as when Step 6 first shipped them — zero new issues). `npx vitest run` → 726/726 (one unrelated flake in `WorkspaceGanttView.test.tsx` from running the full suite and a production build concurrently, confirmed by re-running that file alone: 28/28). Extended `PeopleCell.test.tsx` with 5 new guard tests (fires only when `hasAccess` is false; "Assign anyway" skips `grant()`; "Grant & assign" calls `grant()` then assigns; "Grant & assign" hidden when `canGrant` is false; no guard when the colleague already has access) — 14/14 total, all passing alongside the untouched private-workspace tests. Live Playwright against disposable shared and private scratch boards (separate production server on port 3011 again, `next dev`'s per-project lock still applies): on the shared board, picking a team member without a `board_members` row correctly showed the guard, not a silent assignment; "Grant & assign" produced a real `board_members` row and the correct `column_values` write, confirmed directly against the DB; the private board's picker still showed only the signed-in user. `npm run test:rls` → still 37/37. Scratch fixtures deleted.

- [x] **Step 10: Activity Feed Immutable Audit Ledger**
  - **Files:** `supabase/migrations/20260910140334_audit_snapshot.sql` (new), `components/views/ActivityLog.tsx`, `hooks/useRealtimeSync.ts`, `app/page.tsx`, `types/index.ts`, `lib/i18n/en.ts`, `lib/i18n/fr.ts`, `__tests__/hooks/useRealtimeSync.test.ts`
  - **Pre-flight (per `.cursorrules`):** Passed. `postgres_changes` is already the established realtime pattern (6 files, incl. `useRealtimeSync.ts` itself with a `board_id=eq.` filter) so the subscription slots into the existing board-scoped channel — no new mechanism, no new state manager (constraint #3 holds). `audit_logs` RLS (`is_global_admin() AND can_access_board`) is enforced by Realtime, so INSERT events only reach admins with board access (constraint #1 holds). Verified `audit_logs` was **not** in the `supabase_realtime` publication, so the migration adds it.
  - **Logic:**
    - **Database:** `20260910140334_audit_snapshot.sql` adds `item_name_snapshot TEXT` and rewrites both `SECURITY DEFINER` triggers to write `NEW.name` into it on every insert path (`item_created`, `item_deleted`/`item_restored`, `name_changed`, and each `column_values` field-change row). The 52 pre-existing rows are backfilled from the live `items.name` (0 orphaned at the time), falling back to `old_value`/`new_value ->> 'name'` — required so removing the join doesn't blank historical rows. Migration also runs `ALTER PUBLICATION supabase_realtime ADD TABLE public.audit_logs`.
    - **Frontend Sync:** `useRealtimeSync.ts` gained an `onAuditLogChanged?` callback and an `.on("postgres_changes", { event: "INSERT", table: "audit_logs", filter: board_id=eq.${boardId} })` on the existing board channel (so its lifecycle already matches "a board is open", which is the only time the admin Activity tab can be shown). `app/page.tsx` wires it to `queryClient.invalidateQueries({ queryKey: queryKeys.auditLogs(boardId) })`, same pattern as the other four realtime callbacks.
    - **UI Rendering:** `ActivityLog.tsx` query is now `.select("*")` (no `items(name)` join); `describeLog` reads `log.item_name_snapshot || t("audit.deletedItem")`. `types/index.ts` `AuditLog` drops `items?: { name }` and adds `item_name_snapshot: string | null`.
    - **Smart Formatting:** Added a dedicated `name_changed` branch → new `audit.nameChanged` key ("{actor} changed the name from \"{oldValue}\" to \"{newValue}\"" / fr "{actor} a renommé « {oldValue} » en « {newValue} »"), no `{item}` clause. `item_deleted` and other field changes already render from `itemName`, which is now the snapshot.
  - **Verification:** `npx tsc --noEmit` and `eslint` clean on all touched files (only the pre-existing `any`/exhaustive-deps baseline, nothing on new lines); `npm run build` green. **Trigger test** (SQL `DO` block, throwaway `ZZ Step10 Verify` workspace): a create → rename → status-change → soft-delete sequence produced 4 audit rows with `item_name_snapshot` = `S10 Original`, `S10 Renamed`, `S10 Renamed`, `S10 Renamed` respectively — snapshot captured at write time on every path. Confirmed `audit_logs` is in `supabase_realtime` and all 52 legacy rows backfilled (`with_snapshot = 52`). **Cache-invalidation test:** new case in `useRealtimeSync.test.ts` asserts the `audit_logs` INSERT subscription is registered with the right filter and its handler calls `onAuditLogChanged(boardId)` (which is the `invalidateQueries` wiring) — no real socket, per the DO-NOT-TEST rule. `npx vitest run` → 722/728 with 6 `WorkspaceGanttView.test.tsx` timeouts, all confirmed as the known full-suite-plus-build concurrency flake (28/28 in isolation); `useRealtimeSync.test.ts` + `PeopleCell.test.tsx` → 19/19. `npm run test:rls` → 37/37. Scratch fixtures deleted. No browser automation tool was available this session, so the live Activity-tab render was not re-verified in a browser.
  - **Testing Rules:**
    - **DO TEST:** Test the Postgres triggers to ensure `item_name_snapshot` is correctly populated. Verify that the TanStack Query cache invalidates automatically when a new log row is inserted.
    - **DO NOT TEST:** Do not write complex E2E tests for the real-time socket connections. Do not over-engineer the UI formatting logic; use simple string interpolation.
  - **Directives:** Do not refactor outside this scope. Do not auto-generate massive files; edit surgically.

---

## 5. Strict Constraints (Anti-Patterns)

1. **Do not bypass RLS for Audit Logs:** Never use the `SUPABASE_SERVICE_ROLE_KEY` to fetch audit logs for the UI. Admins must never see private workspace logs; RLS is the single source of truth for this.
2. **Do not use `pg_cron` for the purge job:** Because Supabase Storage attachments must be deleted, a pure SQL cron job is insufficient. You must use the Next.js API route as instructed so the Supabase JS client can handle the HTTP storage deletions.
3. **Do not introduce new state managers:** Stick exclusively to TanStack Query. Do not add Redux, Zustand, or Context for caching. Use the `BroadcastChannel` pattern exactly as specified to trigger query invalidations.
